#include <stdio.h>
#include <stdlib.h>

int fibo(int p, int arr) {
	if(p == 1)
		return 0;
	if(p == 2)
		return 1;
	return fibo(n-1) + fibo(n-2);
}

int main() {
    
    int n, resp; 
    
 
    printf("Digite um numero para a fibonacci: ");
    scanf("%d", &n);
    
    

    //the destroyer of codes
}


